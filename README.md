# Laravel 7 & Angular 9 CRUD Example 
  
* Backend : 
 
composer install
 
Edit .env with the data of the connection to mysql
 
* Frontend : 
 
npm install
 
Edit target in proxy.config.json with backend url
